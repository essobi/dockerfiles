# We base our image on Ubuntu 12.04 (Precise Pangolin) with Java 8 installed  
FROM rickw/ubuntu12-java8  
  
# Download latest package list (required)  
RUN apt-get update  
  
# Install Maven  
RUN apt-get -y install maven  

