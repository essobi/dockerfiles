FROM triangle/ubuntu-saucy-with-rvm  
MAINTAINER zs <baloghzsof@gmail.com>  
  
# install ruby 2.0  
RUN /bin/bash -l -c "rvm install 2.0.0"  
  
# use ruby 2.0 as default  
RUN /bin/bash -l -c "rvm use 2.0.0 --default"  

