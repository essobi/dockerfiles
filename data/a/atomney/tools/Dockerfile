FROM ubuntu:latest  
ENV TERM xterm  
RUN apt update && apt install -y git net-tools mtr-tiny ssh wget curl unzip
iputils-ping iperf tmux screen dnsutils snmp nmap telnet libpam-google-
authenticator xterm sshfs  
EXPOSE 22  

