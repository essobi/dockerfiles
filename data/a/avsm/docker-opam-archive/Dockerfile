# Autogenerated by OCaml-Dockerfile scripts  
FROM avsm/docker-opam-build:ubuntu-14.04-ocaml-4.02.1  
MAINTAINER Anil Madhavapeddy <anil@recoil.org>  
RUN sudo -u opam sh -c "cd /home/opam/opam-repository && git pull origin
master"  
RUN sudo -u opam sh -c "opam update -u -y"  
RUN sudo -u opam sh -c "OPAMYES=1 OPAMJOBS=2 opam depext -u lwt tls cohttp"  
RUN sudo -u opam sh -c "OPAMYES=1 OPAMJOBS=2 opam install lwt tls
cohttp.0.18.0"  
RUN sudo -u opam sh -c "cd /home/opam/opam-repository && opam-admin make"  
ONBUILD RUN sudo -u opam sh -c "cd /home/opam/opam-repository && git pull &&
opam-admin make"

