FROM muffato/ensembl-apis-linuxbrew  
  
# Run the install several times in case it is incomplete  
RUN brew install ensembl/cask/compara-proteintrees  
RUN brew install ensembl/cask/compara-proteintrees  
RUN brew install ensembl/cask/compara-proteintrees  
  

