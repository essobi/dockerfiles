FROM golang:1.9  
MAINTAINER YAMASAKI Masahide <masahide.y@gmail.com>  
  
RUN apt-get update && apt-get install -y --no-install-recommends \  
jq bash openssh-client git tar unzip wget \  
&& rm -rf /var/lib/apt/lists/*  

