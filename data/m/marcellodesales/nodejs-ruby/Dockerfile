FROM marcellodesales/ruby2  
MAINTAINER Marcello_deSales@intuit.com  
  
# Install the nodejs dependencies  
RUN apt-get update && apt-get install -y nodejs npm && ln -s /usr/bin/nodejs
/usr/bin/node && apt-get clean  

