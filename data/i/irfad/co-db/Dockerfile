# docker build -t co-db .  
# docker stop co-db && docker rm co-db  
# docker run -d --name co-db -p 3306:3306 -v "/opt/mysql:/var/lib/mysql" -v
"/root/.my.cnf:/root/.my.cnf" -e MYSQL_RANDOM_ROOT_PASSWORD=yes -e
MYSQL_USER=crossover -e MYSQL_PASSWORD=secret -e MYSQL_DATABASE=crossover
irfad/co-db  
# docker exec -ti co-db /bin/bash  
  
from mariadb  
  
maintainer Irfad Razick  

