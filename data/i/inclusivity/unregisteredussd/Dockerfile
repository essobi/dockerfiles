# change this one with your own base image  
# to generate your own base image  
# do - (docker build -t {your repo name} -f BaseDockerFile . )  
# then push your image - (docker push {your repo name}  
FROM mwaaas/ussd_airflow_seed_proj:baseimage_master

