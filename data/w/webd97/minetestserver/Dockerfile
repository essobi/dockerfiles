FROM alpine  
# This is actually just a mock Dockerfile to avoid Docker Cloud failing the
build if it can't find the default Dockerfile.  
# We're actually using the Dockerfiles in Dockerfiles/ which provide platform-
specific base images.  

