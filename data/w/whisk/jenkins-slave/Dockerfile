FROM java:8u45-jdk  
RUN groupadd -g 1000 jenkins_slave  
RUN useradd -d /home/jenkins_slave -s /bin/bash -m jenkins_slave -u 1000 -g
jenkins_slave  
RUN apt-get update && apt-get install -y docker.io fabric python-pip python-
dev  

