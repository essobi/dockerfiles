FROM eboraas/apache-php:latest  
  
MAINTAINER Christophe Boucaut <boucaut.christophe@gmail.com>  
  
RUN apt-get update && apt-get -y install curl php5-curl && apt-get clean && rm
-rf /var/lib/apt/lists/*

