  
#kali linux container for general purpose  
# run e.g: apt-get install kali-linux  
# or kali-linux-full or similar...  
  
FROM kalilinux/kali-linux-docker:latest  
MAINTAINER fugitivus <fugitivus@gmx.net>  
  
#environment-variables for non-interactive shell:  
#ENV DEBIAN_FRONTEND noninteractive  
  
#install updates, some packages and do cleanup:  
#RUN apt-get update && apt-get upgrade -y\  
# && apt-get install htop bmon aptitude -y\  
# && apt-get clean autoclean\  
# && apt-get autoremove -y\  
# && rm -rf /var/lib/{apt,dpkg,cache,log}/  

