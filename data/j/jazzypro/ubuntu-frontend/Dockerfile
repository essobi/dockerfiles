FROM jazzypro/ubuntu-backend-node:latest  
  
ENV HOME /root  
ENV DEBIAN_FRONTEND noninteractive  
  
#  
# Install PhantomJS  
#  
RUN apt-get install -y libfontconfig  
RUN npm install -g phantomjs-prebuilt  

