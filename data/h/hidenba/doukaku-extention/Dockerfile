FROM ubuntu:16.04  
  
RUN apt-get update && apt-get install -y sudo curl file wget ruby haskell-
platform openjdk-9-jdk golang && curl -sSf https://static.rust-
lang.org/rustup.sh | sh  
  
WORKDIR /doukaku  

