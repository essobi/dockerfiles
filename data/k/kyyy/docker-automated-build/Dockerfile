# Install a more up to date mongodb than what is included in the default
ubuntu repositories.  
FROM ubuntu  
  
RUN apt-get update  
RUN apt-get -y install apt-utils  
  
#RUN echo "" >> /etc/mongodb.conf  

